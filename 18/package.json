{
  "name": "severin-marcombes-static",
  "version": "1.0.0",
  "private": true,
  "description": "Static HTML build of severin-marcombes.fr. Deploy the dist/ folder as-is; dist/styles.css is committed, so no build step is required.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/styles.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./dist/styles.css --watch",
    "serve": "python3 -m http.server 4318 --directory dist"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "tailwindcss": "^4.1.18"
  }
}
